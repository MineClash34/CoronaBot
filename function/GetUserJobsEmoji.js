module.exports = function(Jobs) {
        if (Jobs === "Mineur") return "⛏️"
        if (Jobs === "Bucheron") return "🪓"
        if (Jobs === "Joaillier") return "🔬"
        if (Jobs === "Forgeron") return "🔨"
}